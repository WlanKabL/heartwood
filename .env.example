# Required: the bearer token Claude Code must send to use the server.
HEARTWOOD_TOKEN=change-me

# Optional: HTTP port (default 8722).
PORT=8722

# Optional: SQLite file path, or :memory: for ephemeral (default ./heartwood.db).
DB_PATH=./heartwood.db
